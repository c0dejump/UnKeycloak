#!/usr/bin/env python3

from utils.utils import (
    requests,
    sys,
    time,
    args
)
from utils.style import Colors
from modules.defaultkeycloak import default_credentials


def user_enum(s, realms, client_ids):
    pass